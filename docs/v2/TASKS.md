# TASKS — BMM Continuous Prediction Market **V2**

Phased plan for V2. Companion to `V2-TDD.md`. **Prerequisite: V1 shipped and in use** (`TDD.md` / `TASKS.md` done). All V2 work is additive — V1 markets keep behaving exactly as before (`belief_kind='gaussian'`, 1× cash-collateralized).

Legend: `core`/`shared`/`api`/`web` as in V1. Each phase ends with a runnable checkpoint. `[blocked-by]` = hard deps.

> ** Math-doc sync (standing rule).** The interactive math documentation (`docs/math/index.html`) is the public source-of-truth explainer for the model. **After every phase that changes the math** — new belief models, pricing/`dPrice_dMu`, adaptive-parameter rules, reserve/sampler changes — its checkpoint is **not complete** until `docs/math/index.html` is updated to match: add/derive the new formulas, refresh any affected worked examples (re-compute the numbers), extend the relevant background blocks, and keep both Trader and Developer modes consistent with the shipped code. Verify the doc renders (0 tag errors, 0 KaTeX errors) before closing the phase. Phases that touch no math (e.g. scale/ops, transaction ledger) need no math-doc change — note "math-doc: n/a" in the checkpoint.
>
> **KaTeX verification is now runnable locally** (the G-phases deferred it with "katex not installed" — that caveat is retired). The vendored bundle renders under Node despite the repo's ESM default if loaded via `vm` as CommonJS: read `docs/math/vendor/katex/katex.min.js`, `vm.runInNewContext(code, {module, exports, self:{}, window:{}})`, then for each `$$…$$` / `\[…\]` / `\(…\)` extracted from `index.html` call `katex.renderToString(src,{displayMode, throwOnError:true})` and count failures. **Last full pass (2026-06-20): 665 expressions, 0 KaTeX errors, katex 0.16.11; `<details>` 18/18, `<section>` 34/34 balanced.** The doc is in sync with the shipped multi-model engine (all 5 belief models + all 6 extension contracts).

**Recommended order:** V2-1 (beliefs) → V2-2 (adaptive) — builds on beliefs — alongside V2-3 (oracles) → V2-7 (hardening) → V2-8 (belief-history chart, last). The transaction-ledger / admin-history phases (V2-5/V2-6) are independent and already done. **Scale & ops (the former V2-4)** is moved out of V2 to its own `scaling` milestone (`docs/scaling/`) — not needed at dev stage.

> ** Moved to V3.** **Leverage, margin, shorting & liquidation**, the **insurance fund** that backstops them, and **hedging** (MM reserve reduction) are **not part of V2** — they are deferred to **V3** (`docs/v3/`). Without shorting/borrowed exposure there is no margin gap-loss or user bankruptcy for the fund to absorb, so those travel together; **hedging** moves with them as the MM-side risk tool — it lowers the pool's reserve, is independent of leverage, and works at 1×. V2 stays **1×, cash-collateralized** end-to-end; every V2 market behaves as in V1 on the collateral axis. See `docs/v3/TASKS.md` / `docs/v3/TDD.md`, the explainer `docs/v3/shorting-and-leverage.md`, and the hedging companion `docs/v3/HEDGING.md`.

---

## Phase V2-1 — Multi-modal beliefs `[core, api, web]` (Workstream A) DONE (mixture-first, 2026-06-11)
**Goal:** markets can run on Gaussian Mixture or Student-t, fully priced.
- [x] `core/mixture.ts`: `MixtureBelief implements BeliefModel` (sample/pdf/cdf/quantile/mean/variance/serialize). 10 unit tests.
- [x] `core/student_t.ts`: `StudentTBelief implements BeliefModel` (location-scale t; self-contained lgamma + incomplete-beta CDF + Marsaglia–Tsang gamma sampler). 13 tests.
- [x] `pricing.ts`: mixture price = Σ π_k·componentPrice (reuses V1 closed forms, exact); t price via the existing Simpson quadrature fallback; `dPrice_dMu` generalized (mixture = Σ π_k·∂Price_k/∂μ_k, t via central difference); `expectF`/`signal`/`spread`/`stats` all made belief-kind-agnostic.
- [x] `bayes.ts` + new `mixture_ops.ts`: `bayesUpdateMixture` (responsibility-weighted per-component update + tempered weight/membership update, log-space) + kind-agnostic `updateBelief` dispatcher; prune / moment-match merge / K-cap / (optional) split.
- [x] `solvency.ts`: already MC; mixture/t route through `belief.sample()` automatically (sampler per kind). Confirmed by `solvencyMixture.test.ts`.
- [x] `api`: `markets.belief_state` jsonb column (migration `0003_colorful_hedge_knight.sql`); `lib/belief.ts` (`loadBelief`/`beliefPersistFields`); market creation accepts `belief` (mixture components); trade path (`quote`/`executeTrade`/`sellAll`), `marketView`, `statsSvc`, `lpSvc`, `marketLedgerSvc` all kind-agnostic. `MarketView.belief` exposes components.
- [x] `web`: belief chart renders the general multi-bump pdf (`mixturePdf`/`mixturePdfCurve`) + per-component mode markers with live weight %; admin creation editor for mixture modes (`buildCreateMarketBody`).
- [x] **Tests:** mixture price = weighted component sum + vs MC; merge/prune conserve mass+mean; t price vs MC; weight concentrates on consistent-signal component; mixture create+trade API integration (`mixtureMarket.test.ts`); web `mixturePdf` + create-body tests. **Repo: core 178, shared 9, web 144, api 124 — all green; typecheck + biome clean.**
- [x] *Fast-follow (2026-06-11): Student-t **now genuinely end-to-end**. Surfaced the admin creation control (third "Student-t (fat tails)" belief-shape toggle + ν editor; `createBeliefSchema` `student_t` variant; `marketSvc` builds it via `StudentTBelief.fromVariance(ν, μ, σ²)`). **Also fixed a latent gap the earlier "end-to-end" note missed:** `updateBelief` had no `student_t` branch, so the first trade on a t-market 500'd (`updateBelief: unsupported belief kind student_t`). Added `bayesUpdateStudentT` (precision-weighted update in the variance domain, ν held fixed — degrades to the Gaussian update as ν→∞). Pricing/`dPriceDMu` already handled t. **Chart now draws the true fat-tailed t curve** (`studentTPdf`/`studentTPdfCurve` in web `viz.ts`, peak-normalised kernel — no lgamma needed since the axis is relative likelihood; ν exposed on `MarketView.belief.nu`; `BeliefChart` dispatches on `beliefKind`). **Math-doc synced:** added the "V2 — updating a Student-t belief" derivation to `docs/math/index.html` §7 (variance-domain Normal–Normal step, ν fixed, → Gaussian as ν→∞); 443 expressions render, 0 KaTeX errors. Tests: core `bayesUpdateStudentT` (5), web build-body + ν validation (2) + `studentTPdf` shape/fat-tail/ν→∞ (4), api integration create→quote-vs-MC→trade-updates-belief (3).*
- [x] *Fast-follow (2026-06-11): **closed the last 3 client-side preview gaps** that still assumed Gaussian. The authoritative trade/quote path was already kind-correct, but three no-round-trip live previews reconstructed `new GaussianBelief(μ,σ²)` regardless of kind — so on a mixture/Student-t market the live fair estimate (`clientQuote.estimateQuote`), the projected post-trade belief (`clientBelief.projectBelief`, which also wrongly used the Gaussian `bayesUpdate` instead of the kind-aware `updateBelief`), and the fair-price-vs-strike sweep (`PriceCurveChart`) all approximated. Added `lib/beliefFromView.ts` (rebuilds the real `MixtureBelief`/`StudentTBelief`/`GaussianBelief` from `MarketView.belief`, mirroring the API `loadBelief`); `QuotePanel` memoises it and passes the real model to both previews; `MarketPage` passes `m.belief` to `PriceCurveChart`. Now every preview prices/projects against the market's true belief. Tests: `beliefFromView` (4), updated `clientQuote` (mixture+t fair correctness) and `clientBelief` (t dispatches via `updateBelief`; mixture projection diverges from the old Gaussian path). web 161 green; build clean; typecheck 4/4; all 3 belief kinds now exact end-to-end including live previews. **Perf:** making the previews kind-exact meant a Student-t market now prices via quadrature (4000-node Simpson, no closed form); `PriceCurveChart` re-swept all 80 points every drag frame → ~320k pdf evals/frame → chart lag while dragging a handle. Fixed by memoising the sweep on what actually shapes the curve (belief, domain, non-swept spec params) — the price-vs-strike curve is invariant to the swept strike, so a drag now re-prices only the single dot (~4k/frame); curve recomputes on release / belief change. Strike/center/mid drags fully cached; verified smooth. **Follow-up:** SPREAD / GAUSSIAN-bell contracts DO reshape their price curve as the width handle drags (can't be cached like the strike family), so on a Student-t market those re-swept full quadrature per frame — coarsened the sweep to 22 points while such a drag is live (closed-form kinds stay full-res; snaps back on release).*
- [x] *Fast-follow (2026-06-11): **belief-model sandbox in the math doc** (`docs/math/§belief`) — answers "why do my markets still look Gaussian?". Ported Mixture + Student-t into `math.js` (lgamma, `MixtureBelief`, `StudentT`, `expectF` quadrature, kind-agnostic `priceAny`/`dPriceDMuAny`, `manageMixture`, `bayesUpdateMixture`/`bayesUpdateStudentT`/`updateBelief`; gave Gaussian `Belief` the `mean/variance/stddev` interface) — verified vs MC (mixture Δ1e-4, t Δ5e-4). New `vizBeliefModels` widget (`app.js`): pick Gaussian/Mixture/Student-t, drawn against the equal-(μ,σ) Gaussian so any gap is pure shape; shows fair-price divergence (model vs Gaussian %), and a Buy/Sell-commit that updates each by its own rule (Gaussian slides, mixture concentrates on a camp then merges, t re-centres with ν fixed). Prose clarifies the two reasons markets look Gaussian: kind is fixed at creation (trades never grow modes/tails on a Gaussian market) and a moderate-ν t differs only in the tails. 448 expressions render, 0 KaTeX errors.*
**Checkpoint:** create a bimodal-mixture market, trade it, watch component weights shift live and prices stay consistent with MC. **Math-doc:** added the Mixture/Student-t belief, `Σ π_k·componentPrice` pricing, generalized `dPrice_dMu`, per-component Bayes + weight (membership) update + prune/merge, and the per-kind MC sampler; new worked bimodal `BINARY_CALL` example (0.4213 vs 0.327 moment-matched). 431 expressions render, 0 KaTeX errors.

> ** Reconciliation — V2-1 was superseded/extended out-of-band by the "multi model" effort (`docs/multi model/`, phases G0–G6, COMPLETE 2026-06-18).** That track was run as a separate plan but is effectively **V2-1++**. Net new since the V2-1 checkpoint above:
> - **Two more belief models** beyond Gaussian/Mixture/Student-t: **Gen·basis ** (a spawning Gaussian mixture; `model='gen_basis'`, `belief_kind='mixture'`, adaptive mode-spawning + "paint the curve" placement) and **Gen·exact ** (`belief_kind='gen_exact'`, max-entropy `exp(−poly)`, quadrature-priced, v1 location/scale + **v2 moment-projection** shape-adapting update, default-on `genExactShapeAdapt`). A `markets.model` column (`NOT NULL DEFAULT 'gaussian'`, migration `0004`) carries the creator's model; `belief_kind` stays the math representation. **Five models total.**
> - **Six new contract shapes:** bounded closed-form **SKEW_GAUSSIAN / TENT / TRAPEZOID / SIGMOID** (G5.1) and conditionally-compatible unbounded **POLYNOMIAL / EXPONENTIAL** (G5.2), gated by a real `contractBeliefCompatible` integrability/boundedness guard enforced at quote+trade.
> - Admin create-flow refactor (generals primary, classics behind "More models ▾"); placement interaction; demo seed (`db:seed:markets`); legacy golden-master; **full multi-model lifecycle integration sweep** (`api/test/multiModelSweep.test.ts`).
>
> **Downstream impact on the remaining V2 phases** (folded into each below): **V2-2** must tune across all **5** models (and not collide with the per-model *shape* adaptivity that already exists — `allowSpawn`/`tauSpawn` for gen_basis, `genExactShapeAdapt` for gen_exact); **V2-7** is **partially pre-satisfied** by G6 (the multi-model sweep + golden-master + seed already exist); **V2-8**'s ghost-trail snapshot must serialize the general belief forms (gen_exact `lambdas`, mixture/gen_basis components), which `BeliefStateDTO` already supports. Engine parameters today are **static** (`packages/core/src/config.ts` `makeEngineConfig` fixes them per market from σ₀ + overrides) — no EWMA σ_ε, regime s₀, or `market_cfg_history` exists yet, so V2-2 is unstarted.

---

## Phase V2-2 — Adaptive parameters `[core, api, web]` (Workstream C) `[blocked-by: V2-1]` DONE (2026-06-20)
**Goal:** self-tuning σ_ε and spreads (`MODEL.md §14.2`). Today every market's `EngineConfig` is **static** — `makeEngineConfig(μ₀,σ₀)` fixes σ_ε, s₀, α/β at creation. This phase makes them **track the market's realized signal error / volatility within the §14.1 rails**, logs the trajectory, and lets an admin pin overrides.

> **Scope note (post-multimodel).** This is **MM/pricing-parameter** adaptivity (σ_ε precision, spread width, signal strength) and is **orthogonal to the per-model *shape* adaptivity that already shipped** in G0–G6: gen_basis mode-spawning (`allowSpawn`/`tauSpawn`) and gen_exact moment-projection (`genExactShapeAdapt`) adapt the *belief density*, V2-2 adapts the *engine constants*. Keep them separate — but the new σ_ε feeds **all five** models' Bayesian update (`updateBelief` precision `w/σ_ε²`), so the EWMA must be model-agnostic and verified on a gen_basis / gen_exact market too, not only Gaussian.

- **V2-2a `core` (the math heart) — DONE (2026-06-20):** `packages/core/src/adaptive.ts` — pure, belief-kind-agnostic controller consuming a stream of scalar signal errors `|s − μ_prior|`. EWMA `σ_ε` (debiased by the half-normal factor `√(π/2)` since `E|ε| = σ_ε√(2/π)`), volatility regime from a fast/slow EWMA pair (`regime = clamp(emaFast/emaSlow − 1, 0, cap)`), regime-scaled `s₀ = base·(1+regime)`, and optional `α/β` damping (inverse to the noise ratio; off by default). Every output clamped to `§14.1` rails (σ_ε ∈ [0.1,2]·σ₀ bracketing the static demo default; s₀ ∈ [0.001,0.02]; α∈[0.5,2]; β∈[0.5,1.5]) with per-rail hit flags for the V2-2b breaker. `initAdaptiveState`/`observeError`/`adaptParams` + `DEFAULT_ADAPTIVE`, exported from the barrel. **Tests:** `core/test/adaptive.test.ts` (15) — exact debias recovery, biased-without-correction, RNG recovery, regime tracking/widening, all four rail clamps + hit-reporting, warmup/disabled inertness, purity. Full core suite **431 pass** (was 416 + 15); typecheck 4/4; biome clean. *Not yet wired into the trade path — that's V2-2b.*
- **V2-2b `core`→`api` wiring + persistence + admin + breaker — DONE (2026-06-20):**
  - [x] `api/lib/adaptiveCfg.ts` — `liveEngineConfig(row)` derives the live `EngineConfig` (base + adapted/pinned σ_ε,s₀,α,β) from the **pre-trade** controller state; `foldError` advances the EWMAs from `|s − μ_prior|`; `packCfgState`/`cfgHistoryRow` for persistence + history. Belief-kind agnostic (reads only `belief.mean()`); the adapted σ_ε feeds the shared precision `w/σ_ε²`.
  - [x] Wired into all **three** `tradeSvc` sites: `quote` (read-only live cfg), `executeTrade` + `sellAllPositions` (live cfg for pricing/update, then fold → persist `cfg_state` → append `market_cfg_history` → pass `paramRailHit` to the breaker). Causality: this trade prices on pre-trade params; its surprise tunes the next.
  - [x] `markets.cfg_state jsonb` (EWMA state + admin control) + `market_cfg_history` table (per-fill σ_ε/s₀/α/β/regime/railHit/source), migration `0005_chilly_anthem` (cfg_state nullable, history FK **ON DELETE CASCADE** — derived log, keeps teardown simple); DB reset+reseed.
  - [x] Admin pin/override: `GET /admin/markets/:id/cfg` (live + adapted + history) and `PATCH …/cfg` (enable/pin/cfg-overrides, replace-merge) via `cfgSvc.ts` + `adaptiveControlSchema`; audit row `market_cfg_update`.
  - [x] Rail-hit circuit breaker: new `param_rail` breaker in `@bmm/core` (`breakers.ts`, info→alert) wired post-commit at both trade sites. *(Info alert per §14.1, not auto-SUSPEND — a clamped tuning param is an envelope-edge signal, not a halt condition; the suspend-class breakers stay rapid-move / insolvency.)*
  - [x] **Tests:** `api/test/adaptiveParams.test.ts` (6 — state+history accumulate, live σ_ε feeds the update via pinned hi/lo projection, **Student-t** market adapts (kind-agnostic), disable freezes at baseline, pin overrides + clear, far stream trips the σ_ε rail+breaker); `core/test/breakers.test.ts` +2 (param_rail fires/skips, multi-breaker). Full suite **shared 23 · core 433 · web 190 · api 167**, 0 fail; typecheck 4/4; biome clean.
  - **Math-doc:** §8 gains a "V2 — adaptive parameters" `<details>` (slow/fast EWMA, half-normal `√(π/2)` debias → σ_ε, regime-scaled s₀, optional α/β, §14.1 rails, causality/persistence); §14 gains the `param_rail` breaker row. **687 expressions render, 0 KaTeX errors** (was 665); tags balanced.
- **V2-2c — sim/backtest — DONE (2026-06-20):** extended `packages/core/src/sim.ts` (the §17.3 Monte-Carlo) with three opt-in knobs — `adaptive` (run the §14.2 controller per trade), `strikeAtRead` (traders bet struck at their noisy read so the signal surprise tracks real noise, not the endogenous-to-σ ATM surprise), and `beliefKind` (run the whole zoo via the kind-agnostic `updateBelief`/`extractSignal` path) — plus `compareAdaptiveVsStatic` which runs the identical seeded scenario both ways and reports the calibration/error deltas. Defaults preserve the V1 Gaussian/ATM behaviour byte-for-byte (existing 11 sim tests untouched; `updateBelief`→Gaussian is the same `bayesUpdate`). CLI: `bun run sim --ab [--strike-read] [--kind …]`. **Finding (honest + asymmetric):** adaptation is ~neutral when σ_obs≈σ₀ (static already right) but **materially improves calibration when σ_obs≳2σ₀** (static overconfident calib≈0.48→adaptive≈0.79; the σ_ε rides its 2σ₀ rail, i.e. the param-rail breaker fires) — it earns its keep exactly in the volatile regime. **Tests:** `core/test/sim.test.ts` +5 (adaptive determinism; σ_ε within rails; off-by-default; the volatile calibration-improvement assertion; model-zoo smoke across all 4 kinds). Core **438 pass**. **Math-doc:** §18 gains an "A/B testing the adaptive parameters" `<details>` (the asymmetric finding + why strike-at-read). 696 expressions, 0 KaTeX errors.
- **V2-2d — `web` — DONE (2026-06-20):** an **Adaptive params** sub-tab on each admin market (alongside Overview / Cash-flow ledger). `AdaptiveParamsView` shows the live σ_ε/s₀/α/β vs their static baseline (accent when moved), the controller state (obs count, σ₀, regime) + a `rail:` badge when a §14.1 clamp binds, σ_ε & s₀ history sparklines, and controls to toggle adaptation or pin σ_ε/s₀ (replace-merge; clear-pins). Wiring: `lib/cfgView.ts` (pure `sparkPoints`/`cfgSeries`/`railSummary`/labels), `api.adminMarketCfg`/`adminSetMarketCfg`, `useAdminMarketCfg` + `qk.adminCfg`. **`param_adapted` WS:** the api publishes it on the market topic after each fill that moved the controller (executeTrade + sellAll, skipped when source='static') and on the admin PATCH; `useMarketSocket` invalidates `qk.adminCfg` on it, so an open view updates live. `param_rail` added to the `AlertsBanner` kind labels + the web `SystemAlert` union. **Tests:** `web/test/cfgView.test.ts` (10 — sparkline empty/single/constant/monotone/bounds, series, pins, rail summary, labels). Web **200 pass**; build clean; typecheck 4/4; biome clean.
**Checkpoint:** adaptation runs end-to-end (core → api → web), self-tunes within rails, and the A/B sim (V2-2c) shows calibration improving vs static in the volatile regime (incl. a non-Gaussian model). **Math-doc:** §8 (controller) + §14 (param_rail) + §18 (A/B) — 696 expressions, 0 KaTeX errors.

> ** Phase V2-2 COMPLETE (2026-06-20).** V2-2a (core controller) · V2-2b (api wiring + persistence + admin + breaker) · V2-2c (sim/backtest) · V2-2d (web) all shipped. Engine parameters self-tune within the §14.1 rails across all 5 belief kinds; admins can chart, pin, or disable per market.

---

## Phase V2-3 — Oracle resolution modes & disputes `[shared, api, web]` (Workstream E) DONE (2026-06-23)
**Goal:** every market resolves through one of **three explicit oracle modes** (`MODEL.md §11`). Replaces the V1 "admin types θ*" path with a first-class, per-market oracle assignment.

> ** Plan revised (2026-06-23) — supersedes the original "OracleSource + ApiFeed/WeatherApi/Manual/Aggregated adapters + median aggregation" sketch, which did NOT match the intended product.** The real design is **three resolution modes chosen per market at creation**, not a bag of pluggable feed adapters with median aggregation. Aggregation/weather/election feeds are dropped from V2. The decentralized mode is a **placeholder only** this phase.
>
> **The three modes** (`market.oracle_mode`):
> 1. **`centralized`** — a specific account with **`Role = oracle`** is assigned as the market's oracle at creation. After the market's deadline (`resolves_at`), **only that oracle account (or an admin) may resolve it**, from a dedicated **Oracle panel**. No scheduling — it's a manual human resolution gated by role + deadline. This is the closest analogue to V1's manual resolve and is the **default/migration mode** (assigned to the admin) for non-crypto markets.
> 2. **`api`** — automatic. When `resolves_at` is reached, a **scheduled job** fetches the configured token's price from the in-house **xprices** price service (`https://xprices.umbralabs.io`, spec `github.com/pydea-rs/pyth-pal`) and resolves the market with that value as θ*. Only valid for **crypto-price markets** (gated by a non-null `oracle_token`, e.g. `BTC`). A **stale/missing/unreachable** feed at resolution time → **SUSPEND + alert** (`§15.1` oracle-failure breaker), retried on the next tick — never resolve on bad data.
> 3. **`decentralized`** (e.g. UMA) — **placeholder only.** The enum value + create-form option exist; the resolution path throws `not implemented` (UMA integration deferred to a later phase). Do not build it now.
>
> **xprices API:** `GET /prices/{token}` → `{ token_id, price, ema_price, confidence, timestamp, stale? }`; no auth. `stale:true` or a missing/old `timestamp` ⇒ treat as feed failure. Health at `GET /health`.
>
> **Scheduler rule:** use the **`cron`** package (a real scheduled job) — **never `setInterval`/`setTimeout` in the backend** (none exist today; this is a forward rule). Front-end timers are fine. The scheduler is **single-node** for now; the `scaling` milestone (`docs/scaling/` S-2) replaces it with a leader-elected/distributed runner — noted as a handoff there.

  - [x] `shared`: `OracleMode` enum (`centralized | api | decentralized`); add **`oracle`** to `UserRole`. DTOs: `oracleMode`, `oracleUserId?`, `oracleToken?` on create/market views; `OracleResolveInput`, `DisputeInput`/`DisputeView`.
  - [x] `schema.ts`: `markets` += `oracle_mode` (varchar, default `centralized`), `oracle_user_id` (FK users, null = admin-resolved), `oracle_token` (varchar, null unless `api`), `resolved_at`, `dispute_window_sec` (default 86400). **Extend the existing `oracles` table as the report log** (add `token`, `stale`; it already has source/value/confidence/disputed/reportedAt) rather than a parallel `oracle_reports`. New `disputes` table (filer, reason, status `open|upheld|rejected`, proposed/secondary value, resolver, timestamps). Migration; reseed (dev-stage, no back-compat).
  - [x] Validation: `api` mode ⇒ `oracle_token` required + market is crypto; `centralized` ⇒ `oracle_user_id` must be a `role=oracle` (or admin) user; `decentralized` ⇒ accepted but resolution throws.
  - [x] `xprices` client lib (`GET /prices/{token}`, stale/missing detection, timeout, typed result). Config base-URL via env.
  - [x] `cron` job: scan `api`-mode markets past `resolves_at` & still OPEN → fetch price → resolve (θ* = price), write `oracle_reports` row. Stale/unreachable → SUSPEND + `oracle_failure` breaker alert; retry next tick. Idempotent under the per-market lock.
  - [x] Add `oracle_failure` breaker kind to `packages/core/src/breakers.ts` (§15.1) + wire its alert.
  - [x] Admin can grant/revoke the `oracle` role; assign an oracle user at market create.
  - [x] `GET /oracle/markets` (markets assigned to the caller, past deadline, awaiting resolution) + `POST /oracle/markets/:id/resolve` (θ*). AuthZ: caller is the assigned oracle **or** admin; only when `now ≥ resolves_at`. Records an `oracle_reports` row (source `centralized`).
  - [x] On resolve (any mode): status → RESOLVED, `resolved_at = now`, claims **computed** (as today) but **credit gated**. The dispute window lives in RESOLVED.
  - [x] User `POST /markets/:id/dispute` (holders only, within window) → opens a `disputes` row, blocks auto-settle.
  - [x] Admin dispute resolution: **uphold** (override θ* via secondary value / manual → re-run `recordClaims`, finalize) or **reject**. `cron` auto-settles RESOLVED→SETTLED once the window passes **and** no dispute is open. `claimPayout` stays gated on SETTLED (no claim-path change needed — gating falls out of the lifecycle).
  - [x] Create-market form: oracle-mode selector (+ oracle-user picker for `centralized`, token field for `api`, disabled `decentralized` placeholder).
  - [x] **Oracle panel** (role-gated): list assigned markets past deadline + resolve action.
  - [x] Admin: oracle/role management + **dispute queue** (uphold/reject/override). User: `dispute` action on a RESOLVED market within window.
- **V2-3f — Tests + seed `[api, web]`:** `api/test/oracles.test.ts` (6): API-mode resolves from a **mocked/injected** xprices fetch (deterministic, no live network in CI); stale feed suspends + alerts + auto-retries; centralized resolve authZ (assigned oracle , non-oracle 403, before-deadline 409); dispute blocks auto-settle & claims, admin override re-pays correctly, window-pass auto-settles; zero-window auto-settles immediately. Web `derive.test.ts` (+4): oracle-mode body assembly. Seeded an `oracle`-role user + an `api`-mode BTC market + a `centralized` market (assigned to oracle). **math-doc: n/a** (no model math changes).

**Checkpoint:** an `api`-mode crypto market auto-resolves from the live/mocked xprices feed at its deadline; a `centralized` market is resolved by its assigned `oracle`-role user (not by anyone else, not before deadline); a disputed resolution is overridden by an admin before claims open; a stale feed suspends instead of mis-resolving.

> ** Phase V2-3 COMPLETE (2026-06-23).** Three oracle resolution modes shipped end-to-end: `centralized` (a `role=oracle` account resolves its assigned markets from a dedicated Oracle panel, post-deadline), `api` (a `cron` job auto-resolves crypto markets from the in-house **xprices** feed, suspending + alerting on a stale/missing feed via the new `oracle_failure` breaker), and `decentralized` (placeholder). Post-RESOLVED **dispute window** holds auto-settle (and therefore claims) until it elapses with no open dispute; admins uphold (override θ* + re-pay) or reject from a dispute queue. Backend is the single source of truth via the in-process `cron` scheduler (no backend `setInterval`); the `scaling` milestone (S-2) replaces it with a leader-elected runner. **Verify (2026-06-23):** shared 23 · core 438 · web 204 · api 173 — 0 fail; typecheck 4/4; biome clean; web build ; reseed runs (oracle user + 8 demo markets incl. api+centralized).

---

## ~~Phase V2-4 — Scale & ops `[api]` (Workstream G)~~ MOVED → `docs/scaling/`
> ** Moved out of V2 (2026-06-23).** Horizontal scale is **not needed at the current dev stage**, so the whole scale/ops effort was carved into its own **`scaling` milestone** (`docs/scaling/TASKS.md`), phased there as S-1…S-5: Redis-backed per-market distributed lock + WS pub/sub fan-out (S-1), leader-per-market routing + leader-elected scheduler (S-2), read replicas + hot cache (S-3), gateway rate-limiting/metrics/health probes (S-4), and multi-node consistency/chaos/load hardening + ops runbook (S-5). The single-node in-process implementation (queue, WS broadcaster, `cron` scheduler) stays the default; everything in that milestone is opt-in behind config flags. **V2 no longer depends on this phase.**
>
> ~~**Goal:** horizontal scale with sequential-consistency preserved (`MODEL.md §18.2`). Redis pub/sub + Redlock distributed lock; consistent-hash leader-per-market; Postgres read replicas; gateway rate limiting + metrics + health probes; 2-node consistency test.~~

---

## Phase V2-5 — Transaction ledger & history `[shared, api, web]` (Workstream I) `[blocked-by: none]`
**Goal:** a single source-of-truth ledger that records every cash movement, surfaced to each user as a "Transactions" tab with filtering, sorting, and lifetime stats. Additive — no change to how cash actually moves; we just record it.

**Design:** a `transactions` table written **atomically inside the same `db.transaction()`** as each balance/cash mutation (the existing post-commit `audit_events` is incomplete and can't be the source). User-centric: each row belongs to a `userId` (whose history it is) with a signed `amount` (+ into wallet, − out), `balanceAfter` (null for infinite/admin accounts), `marketId`, `counterpartyId`, `refType`/`refId`, and `metadata` jsonb. Admin funding writes **two** rows (`admin_credit` on the target, `admin_grant` on the admin) so it shows in both histories. Kinds: `trade_buy, trade_sell, market_create, lp_deposit, lp_withdraw, lp_claim, claim, refund, admin_credit, admin_grant`.

- **TX-1 — Backend ledger `[shared, api]`:** DONE (2026-06-09)
  - [x] `shared`: `TransactionKind` enum (10 kinds). (`TransactionView` DTO deferred to TX-2 with the read endpoint.)
  - [x] `schema.ts`: `transactions` table (indexed `(user_id, created_at)`, `market_id`); migration `0002_eager_echo.sql` applied; added to `schema` export.
  - [x] `services/ledgerSvc.ts`: `recordTx(exec, entry)` / `recordTxs` — insert inside the caller's transaction (executor = `Tx` or `db`).
  - [x] Wired into every cash path: `tradeSvc.executeTrade` (trade_buy/sell), `marketSvc.createMarket` (market_create) + cancel `settleSvc.refundPositions` (refund), `lpSvc.deposit/withdraw/claim`, `settleSvc.claimPayout` (claim), admin top-up via new `services/fundingSvc.ts` (admin_credit + admin_grant; replaced dead `repos.credit`).
  - [x] **Tests:** `test/ledger.test.ts` — 6 integration cases (topup two-row, market_create, trade_buy, lp_deposit, claim, cancel refund); updated every suite's + `demo` cleanup to delete `transactions` first (FKs are RESTRICT). Suite: api 89, all packages green (shared 9 + core 117 + web 71 + api 89); typecheck 4/4; lint clean.
  - **Checkpoint:** ledger rows written atomically per cash path; new integration tests pass.
- **TX-2 — Read API + stats `[api]`:** DONE (2026-06-09)
  - [x] `GET /users/me/transactions` (`routes/users.ts`) → caller's history newest-first (joined market title + counterparty username) + `summary` (count, funded, claimed, tradeBuy, tradeSell, lpDeposited, lpWithdrawn, refunded, net).
  - [x] `services/ledgerView.ts`: `getUserTransactions` + pure `summarizeTransactions` (response shapes `TransactionView`/`TransactionSummary` defined here, mirrored in web at TX-3 per repo convention).
  - [x] **Tests:** `test/ledgerView.test.ts` (pure summarize, 3) + integration GET case in `ledger.test.ts` (scoping, newest-first ordering, summary ≥ this-suite totals, counterparty/title joins). Purged 72 orphaned `(test)` markets from the dev DB. Suite api 93, all packages green; typecheck 4/4; lint clean.
- **TX-3 — Frontend Transactions tab `[web]`:** DONE (2026-06-09)
  - [x] Route `/transactions` (under `RequireAuth`) + nav link after Portfolio.
  - [x] `TransactionsPage` mirroring `PortfolioPage`: stats header (Entered platform / Claimed / Trade volume / Net), category chips + free-text search + sort select, ledger table (kind badge, market link or funding counterparty, signed amount, balanceAfter (∞ for admin), relative time), persistent `transactions.category`/`transactions.sort`.
  - [x] `lib/txView.ts` (pure: sort keys, category buckets, labels, filter/sort); `lib/types.ts` `Transaction`/`TransactionSummary`/`UserTransactions`; `lib/api.ts` `transactions()` + `hooks/queries.ts` `useTransactions`; formatting via `fmt`/`fmtSigned`/`timeAgo`.
  - [x] **Tests:** `test/txView.test.ts` (8) — category/label maps, filter (category + text + compose), sort (recent/oldest/amount, no-mutate). web 79 pass; build 123 kB gzip.
  - **Checkpoint:** a user opens Transactions and sees every action with lifetime stats; admin's grants and the funded user's credit each appear in their own history.

**V2-5 status:** TX-1/TX-2/TX-3 all — transaction history feature complete end-to-end.

---

## Phase V2-6 — Admin market history & panel restructure `[api, web]` (Workstream I) `[blocked-by: none]`
**Goal:** an **admin-only** per-market cash-flow history (the market pool's "bank statement") plus a reorganized admin panel split into focused tabs. No new write-path: the market ledger is **reconstructed by aggregation** from data we already store — every trade's premium flow (`trades.totalCost` in/out of `markets.cash`), the genesis reserve / LP deposits-withdrawals-claims (`lp_ledger`, including the creator's initial liquidity), trader settlement payouts (`claims`), and cancel refunds. Use whichever of trades vs positions is more efficient and accurate for each figure (trades for flow events, positions for net exposure). User-facing transaction history (V2-5) stays user-side only; this is the complementary admin view.

- **MH-1 — Market ledger read service `[api]`:** DONE (2026-06-09)
  - [x] `services/marketLedgerSvc.ts` (read-only): assembles a time-ordered market cash-flow ledger from `lp_ledger` (genesis via `navBefore=0` + deposits/withdraws/claims) + `trades` (signed `totalCost`) + cancel refunds (the per-trader `transactions` refund rows) + `claims` (trader payouts), each as `{ at, kind, delta (signed on the pool), affectsCash, cashAfter, userId, username, ref, note }`; plus rollups (genesisReserve, premiumIn/Out, lpDeposits/Withdrawals, refunds, traderPayouts, lpClaimsPaid, netPoolChange, currentCash, reserveRequired, nav, cashFinal, `reconciles`). Settlement distributions (trader/LP payouts) carry `affectsCash:false` since by design they don't mutate `markets.cash` (residual computed logically); the cash-affecting deltas sum exactly to `markets.cash`.
  - [x] `GET /admin/markets/:id/ledger` (admin-only) → `{ ledger: { events, rollup } }`.
  - [x] **Tests:** `test/marketLedger.test.ts` — genesis+trades+LP reconstruct & reconcile to `markets.cash`; settlement records payouts without moving cash; cancel refunds reduce the pool & still reconcile; admin-only (403)/404. (api 99 green.)
- **MH-2 — Admin panel restructure `[web]`:** DONE (2026-06-09)
  - [x] Split `/admin` into in-page tabs (persistent `admin.tab`): **Markets** (lifecycle list → per-market Overview / **Cash-flow ledger** sub-tabs), **Users** (list + top-up + each user's tx history via new admin-only `GET /admin/users/:id/transactions`), **Create market**, **System** (alerts + system overview). `RequireAdmin` unchanged.
  - [x] `components/MarketLedgerView.tsx`: rollup `Stat` cards + reconcile badge + filterable/sortable event table; `lib/marketLedgerView.ts` (pure category/label/filter/sort) persisted via `usePersistentState`.
  - [x] **Tests:** `test/marketLedgerView.test.ts` (8) — category/label maps, filter (category + text + compose), sort (recent/oldest/amount, no-mutate). web 87 green; build 126 kB gzip.
  - **Checkpoint:** an admin opens a market and sees its full cash-flow statement reconciling to current pool cash; the admin panel is organized into clear tabs.

**V2-6 status:** MH-1/MH-2 both — admin market-history ledger + tabbed admin panel complete end-to-end.

---

## Phase V2-7 — Hardening & polish `[all]` `[blocked-by: all]` DONE (2026-06-23)
> **Partially pre-satisfied by G6 (multi model effort).** Already shipped: a **full multi-model lifecycle integration sweep** (`api/test/multiModelSweep.test.ts` — create→trade→settle→claim for all 5 models × representative new contracts), a **legacy golden-master** (`core/test/legacyGolden.test.ts`, frozen reprice/re-update values), and the **demo seed** (`db:seed:markets`). The multi-modal *belief* calibration is also exercised there. **Remaining for V2-7:** the cross-workstream suite for oracles (V2-3) + adaptive (V2-2), the **adaptive-parameter stability** MC (depends on V2-2), and the ops runbook. *(Load/chaos + multi-node scale tests moved with the scale phase to `docs/scaling/` S-5 — not part of V2-7.)*
- [x] Cross-workstream integration suite (oracles + adaptive); the V1→V2 behavior-preservation guarantee is pinned at the math layer by the existing golden-master (no persisted pre-refactor data, per the no-back-compat stance). *(`api/test/crossWorkstream.test.ts`, 2 tests: an `api`-oracle crypto market **adapts** its engine params as it trades, then the cron tick **auto-resolves** it from a mocked xprices feed and **auto-settles** it — and the adaptive cfg state + history survive the RESOLVED→SETTLED transition intact; a `centralized` market adapts, is oracle-resolved, **disputed**, admin-**overridden**, and settled, with the override re-pay correct on a market that adapted. Pins that the cron-oracle and per-fill-adaptive code paths compose without interfering.)*
- [x] Extended Monte-Carlo sim covering **adaptive-parameter stability** (multi-modal *belief* calibration for mixture/t/gen_basis/gen_exact already covered by the G6 sweep). *(`core/test/sim.test.ts` "adaptive-parameter stability", 5 tests — no core change, pure characterization on the existing §17.3 sim: σ_ε is **bounded** within the §14.1 rails across the whole σ_obs spectrum (never diverges, incl. the neutral σ_obs≈σ₀ zone); a **consistent estimator** — in an interior regime it walks monotonically toward the true σ_obs as evidence accumulates (long horizon strictly closer than short); a **stable saturation** in the deeply-volatile regime (mean final σ_ε varies <5% across a 6.7× horizon range, no drift/oscillation); and a **cross-seed-robust** calibration lift (every seed restores calib₈₀∈(0.7,0.9) from static <0.6). Found + documented the honest caveat: at σ_obs≈σ₀ adaptation is neutral-to-slightly-cautious, so "never harmful" is **not** asserted — only "bounded everywhere, beneficial in the volatile half".)*
- [x] Docs refresh; ops runbook (oracle failure, feed outage, param rail-hit / circuit breaker). *(`docs/v2/RUNBOOK.md` — symptom → cause → remediation for §1 oracle feed failure (self-healing sweep + manual escape hatch), §2 `param_rail` (info-only, pin/disable), §3 the three per-trade breakers, §4 disputes, §5 scheduler-stall; grounded in the actual alert/topic/config mechanics. Indexed in `docs/README.md`. Scale-side failure modes — Redis outage, replica lag, leader flapping — live in the `docs/scaling/` S-5 runbook.)*
- [x] **Final math-doc pass:** audit `docs/math/index.html` end-to-end against shipped V2 code (every formula, constant, and worked example re-computed; both modes consistent; 0 tag/KaTeX errors) — the per-phase syncs land incrementally, this is the consolidating review. *(Audited: **707 expressions, 0 KaTeX errors**, `<details>` 20/20 + `<section>` 34/34 balanced, katex 0.16.11. V2-3 was math-doc n/a (no model math) and V2-7 adds no new math; §18 A/B block gained a **"Stability (V2-7)"** paragraph capturing the boundedness/consistency/saturation/cross-seed findings above.)*
**Checkpoint:** `bun test` green (**core 443 · shared 23 · api 175 · web 204 = 845, 0 fail**; typecheck 4/4; biome clean); V1 markets verified unchanged (legacy golden-master + behavior-preservation suites still green); demo seed exercises beliefs + adaptive params + oracles + disputes end-to-end. *(Load/chaos + multi-node hardening live in the `scaling` milestone S-5; hedging, leverage/liquidation, and insurance hardening live in V3.)*

> ** Phase V2-7 COMPLETE (2026-06-23).** Cross-workstream integration (oracles × adaptive) + adaptive-parameter **stability** MC + the **ops runbook** (`docs/v2/RUNBOOK.md`) + the consolidating **math-doc audit** (707 expr, 0 errors) all shipped. The only remaining V2 phase is **V2-8** (belief-history ghost-trail chart — pure presentation, "do last"). Load/chaos + multi-node scale hardening were moved to the `scaling` milestone (S-5).

---

## Phase V2-8 — Belief-history visualization in the main chart `[web]` `[blocked-by: all]` (Workstream J)
**Goal:** let the **main belief chart** (`apps/web/src/components/BeliefChart.tsx`) show *how the consensus got here*, not just its current shape — encoding **time without a time axis** (the main chart's x-axis is the outcome θ). Pure presentation: no engine/math change. **Do last**, after the whole V2 build is otherwise done.

**Design (decided):** a **ghost trail** — overlay faded snapshots of recent belief PDFs on the same θ-axis, older = more transparent, newest = solid, so the bump visibly drifts and narrows like a comet tail. Time → opacity. Gated behind a small "show history" toggle so the live chart stays clean by default. For V2 mixtures, the ghost is the general multi-modal `pdf(θ)`, so disagreement-then-consensus reads as two bumps fading into one.

**Data dependency (the gating decision):** the ghost trail needs each history point to carry **both μ and σ** (mixture: the full component set) to redraw the past curve. Today `beliefHistory` powers "Belief μ over time" and may store μ only. So:
- [ ] Confirm/extend the belief-history record to log the full belief **snapshot** per sampled point — reuse the existing `BeliefStateDTO` serialization (it already covers all 5 kinds: `μ,σ²` Gaussian; components for mixture/**gen_basis**; `loc,scale,lambdas` for **gen_exact**; `ν` for Student-t), sampled/throttled to keep the series light. The ghost-trail PDF builder must dispatch on `belief_kind` exactly as `BeliefChart` does today (`mixturePdf`/`studentTPdf`/`genExactPdf`).
- [ ] `lib/viz.ts`: pure helper to build N faded PDF polylines from a snapshot series (newest-first opacity ramp; cap the count, e.g. last ~8 snapshots, evenly spaced over the market's life).
- [ ] `BeliefChart.tsx`: render the ghost layer **under** the live PDF/payoff; "show history" toggle (persisted) defaulting off; reuse the existing left-axis likelihood scale (peak-normalise each ghost to the *current* peak so shrinkage is visible).
- [ ] *(Optional, stretch)* a scrubber / play button that animates the PDF morphing past → present (time as the animation clock) and/or a faint "lifetime μ±σ envelope" showing where belief has wandered.
- [ ] **Tests:** the pure ghost-builder (opacity ramp monotone, snapshot cap, ordering, no-mutate); chart renders with 0/1/many snapshots without layout breakage.
**Checkpoint:** toggling "show history" on a live market shows the belief's comet-tail drifting/narrowing over its life; off by default leaves the chart unchanged. **Math-doc:** n/a (presentation only).

---

### Definition of done (V2)
Markets run on Gaussian / Mixture / Student-t beliefs with correct pricing and component management; parameters **self-tune** within spec rails; markets resolve through **three oracle modes** with a **dispute** process; and every cash movement is captured in a reconciling **transaction ledger** (user + admin views). Trading stays **1× cash-collateralized** (no leverage/shorting) — that, **MM hedging**, the **liquidation engine**, and the **insurance fund** are **V3** (`docs/v3/`). **Horizontal scale** (Redis, multi-node, read replicas, rate limiting) is its own **`scaling` milestone** (`docs/scaling/`), deferred until load justifies it. All V1 behavior is preserved for existing markets.
